import { ListingSwiperPipe } from './listing-swiper.pipe';

describe('ListingSwiperPipe', () => {
  it('create an instance', () => {
    const pipe = new ListingSwiperPipe();
    expect(pipe).toBeTruthy();
  });
});
