import { IsSharedOnRunroomiePipe } from './is-shared-on-runroomie.pipe';

describe('IsSharedOnRunroomiePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSharedOnRunroomiePipe();
    expect(pipe).toBeTruthy();
  });
});
