import { ChooseUserPipe } from './choose-user.pipe';

describe('ChooseUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ChooseUserPipe();
    expect(pipe).toBeTruthy();
  });
});
