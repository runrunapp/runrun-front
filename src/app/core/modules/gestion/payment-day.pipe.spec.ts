import { PaymentDayPipe } from './payment-day.pipe';

describe('PaymentDayPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentDayPipe();
    expect(pipe).toBeTruthy();
  });
});
